// note: index.js should always be the page that matches the root route. So this highest level index page is the absolute domain path "domain.com"
const HomePage = () => {
     return <h1>HomePage</h1>;
};
export default HomePage;
